# Railway deployment configuration for web-app
# Build and start commands are set via Railway dashboard/API

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
