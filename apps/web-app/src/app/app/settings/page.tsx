'use client';

import { ProfileSettings } from '@/components/settings/ProfileSettings';

export default function SettingsPage() {
  // Default to profile settings when visiting /app/settings
  return <ProfileSettings />;
}
