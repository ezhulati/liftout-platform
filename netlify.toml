[build]
  command = "npm install && npm run build --workspace=@liftout/web-app"
  publish = "apps/web-app/.next"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "production"

[[plugins]]
  package = "@netlify/plugin-nextjs"
