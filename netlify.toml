[build]
  base = "apps/web-app"
  command = "cd ../.. && pnpm install && cd apps/web-app && pnpm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "production"

[[plugins]]
  package = "@netlify/plugin-nextjs"
