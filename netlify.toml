[build]
  command = "cd apps/web-app && npm install --legacy-peer-deps && npm run build"
  publish = "apps/web-app/.next"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "production"

[[plugins]]
  package = "@netlify/plugin-nextjs"
