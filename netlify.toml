[build]
  base = "apps/web-app/"
  command = "corepack enable && pnpm install --frozen-lockfile=false && pnpm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

[[plugins]]
  package = "@netlify/plugin-nextjs"