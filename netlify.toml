[build]
  command = "npm install -g pnpm && pnpm install && pnpm --filter @liftout/web-app run build"
  publish = "apps/web-app/.next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
