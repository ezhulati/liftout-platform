[build]
  base = "apps/web-app"
  command = "npm install -g pnpm && cd ../.. && pnpm install && pnpm --filter @liftout/web-app run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
