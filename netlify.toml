[build]
  command = "corepack enable pnpm && pnpm install --frozen-lockfile && pnpm --filter @liftout/web-app... build"
  publish = "apps/web-app/.next"
  node_version = "20"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
