[build]
  command = "pnpm install && cd apps/web-app && npm install next@14 --legacy-peer-deps && pnpm run build"
  publish = "apps/web-app/.next"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "production"
  NETLIFY_USE_PNPM = "true"

[[plugins]]
  package = "@netlify/plugin-nextjs"
