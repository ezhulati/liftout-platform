[build]
  base = "apps/web-app"
  command = "pnpm run build"
  publish = ".next"
  node_version = "20"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "production"

[[plugins]]
  package = "@netlify/plugin-nextjs"
